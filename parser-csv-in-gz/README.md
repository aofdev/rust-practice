# Parser CSV in GZ

## Quick Start ⚡️
```bash
# Run with Cargo
cargo run -- datasets/test.csv.gz
```

## Build 🚀
```bash
# build a single
cargo build --release --bin single

# build a parallel
cargo build --release --bin parallel
```